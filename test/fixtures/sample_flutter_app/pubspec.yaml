name: sample_flutter_app
description: Portfolio-chain Flutter fixture for BlastRadius graph and trace tests.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.0.0
  equatable: ^2.0.0
  go_router: ^14.0.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
