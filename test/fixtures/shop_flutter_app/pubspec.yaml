name: shop_flutter_app
description: Catalog-chain Flutter fixture (ChangeNotifier + pages) for BlastRadius.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.0.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
